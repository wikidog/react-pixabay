import React from 'react';
import { AppBar, Toolbar } from 'material-ui';

const NavBar = () => (
  <AppBar position="static" color="default">
    <Toolbar>PixaBay Image Finder</Toolbar>
  </AppBar>
);

export default NavBar;
